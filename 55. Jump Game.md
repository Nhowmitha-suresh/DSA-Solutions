# 55. Jump Game

## Approach
**Greedy**

## Idea
- Maintain the farthest reachable index (`reach`).
- If `i > reach`, return `False`.
- Update `reach = max(reach, i + nums[i])`.
- If the loop completes, return `True`.

**Time Complexity:** `O(n)`  
**Space Complexity:** `O(1)`

<img width="1536" height="1024" alt="image" src="https://github.com/user-attachments/assets/ff2d0669-c63e-450f-88d7-ba509c24226d" />


## Python

```python
class Solution(object):
    def canJump(self, nums):
        reach = 0

        for i in range(len(nums)):
            if i > reach:
                return False

            reach = max(reach, i + nums[i])

        return True
```

## Java

```java
class Solution {
    public boolean canJump(int[] nums) {
        int reach = 0;

        for (int i = 0; i < nums.length; i++) {
            if (i > reach)
                return false;

            reach = Math.max(reach, i + nums[i]);
        }

        return true;
    }
}
```
