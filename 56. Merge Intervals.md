# 56. Merge Intervals

> **Difficulty:** Medium  
> **Topics:** Array, Sorting

<img width="1694" height="929" alt="f4f03c71-12fd-4dbc-ac01-06ea6003113c" src="https://github.com/user-attachments/assets/1a4137f1-3537-4cf8-942c-4f348e28478e" />


## Problem Statement

Given an array of intervals where `intervals[i] = [starti, endi]`, merge all overlapping intervals and return an array of the non-overlapping intervals that cover all the intervals in the input.

---

## Example 1

**Input**

```text
intervals = [[1,3],[2,6],[8,10],[15,18]]
```

**Output**

```text
[[1,6],[8,10],[15,18]]
```

**Explanation**

```
[1,3] overlaps with [2,6]
Merge → [1,6]
```

---

## Example 2

**Input**

```text
intervals = [[1,4],[4,5]]
```

**Output**

```text
[[1,5]]
```

---

## Intuition

If the intervals are not sorted, we cannot determine whether the next interval overlaps with the previous one.

So,

1. Sort intervals by their starting point.
2. Keep the first interval.
3. Traverse the remaining intervals.
4. If the current interval overlaps with the last merged interval, merge them.
5. Otherwise, add it as a new interval.

---

# Approach

### Step 1

Sort the intervals based on the starting value.

```text
Before:
[[2,6],[1,3],[15,18],[8,10]]

After Sorting:
[[1,3],[2,6],[8,10],[15,18]]
```

---

### Step 2

Add the first interval to the answer.

```text
Merged = [[1,3]]
```

---

### Step 3

Compare the next interval.

Current Interval

```text
[2,6]
```

Last merged interval

```text
[1,3]
```

Check

```python
3 >= 2
```

They overlap.

Merge

```text
[1,max(3,6)]

↓

[1,6]
```

Merged becomes

```text
[[1,6]]
```

---

### Step 4

Next interval

```text
[8,10]
```

Check

```python
6 >= 8
```

False

No overlap.

Add it.

```text
[[1,6],[8,10]]
```

---

### Step 5

Next interval

```text
[15,18]
```

Check

```python
10 >= 15
```

False

Add it.

```text
[[1,6],[8,10],[15,18]]
```

---

# Dry Run

Input

```text
[[1,3],[2,6],[8,10],[15,18]]
```

After sorting

```text
[[1,3],[2,6],[8,10],[15,18]]
```

| Current Interval | Last Merged | Overlap | Merged Result |
|-----------------|------------|---------|---------------|
| [1,3] | — | — | [[1,3]] |
| [2,6] | [1,3] | Yes | [[1,6]] |
| [8,10] | [1,6] | No | [[1,6],[8,10]] |
| [15,18] | [8,10] | No | [[1,6],[8,10],[15,18]] |

---

# Algorithm

1. Sort intervals by starting time.
2. Initialize the merged list with the first interval.
3. Traverse the remaining intervals.
4. Compare the current interval with the last merged interval.
5. If they overlap, update the ending value.
6. Otherwise, append the current interval.
7. Return the merged list.

---

# Python Solution

```python
class Solution(object):
    def merge(self, intervals):
        intervals.sort(key=lambda x: x[0])

        merged = [intervals[0]]

        for interval in intervals[1:]:

            if merged[-1][1] >= interval[0]:
                merged[-1][1] = max(merged[-1][1], interval[1])
            else:
                merged.append(interval)

        return merged
```

---

# Java Solution

```java
import java.util.*;

class Solution {
    public int[][] merge(int[][] intervals) {

        Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));

        List<int[]> merged = new ArrayList<>();

        merged.add(intervals[0]);

        for (int i = 1; i < intervals.length; i++) {

            int[] last = merged.get(merged.size() - 1);

            if (last[1] >= intervals[i][0]) {
                last[1] = Math.max(last[1], intervals[i][1]);
            } else {
                merged.add(intervals[i]);
            }
        }

        return merged.toArray(new int[merged.size()][]);
    }
}
```

---

# Complexity Analysis

### Time Complexity

Sorting takes

```text
O(n log n)
```

Traversing all intervals takes

```text
O(n)
```

Overall

```text
O(n log n)
```

---

### Space Complexity

Extra list to store merged intervals

```text
O(n)
```

---

# Key Observations

- Sorting is the key step.
- Only compare with the **last merged interval**.
- Overlap condition:

```python
merged[-1][1] >= interval[0]
```

- Merge using:

```python
merged[-1][1] = max(merged[-1][1], interval[1])
```

- If there is no overlap, simply append the interval.

---

# Pattern

✅ Sorting + Greedy

This pattern is commonly used in interval-based problems.

Related LeetCode Problems:

- 57. Insert Interval
- 252. Meeting Rooms
- 253. Meeting Rooms II
- 435. Non-overlapping Intervals
- 452. Minimum Number of Arrows to Burst Balloons
